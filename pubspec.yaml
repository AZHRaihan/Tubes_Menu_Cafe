name: tubes_menu_cafe
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  # UI & util
  google_fonts: ^6.1.0
  qr_flutter: ^4.1.0
  cupertino_icons: ^1.0.8
  provider: ^6.1.2

  # ─── SQLlite ──────────────────────────────────────────────
  sqflite: ^2.3.2 # database engine
  path: ^1.9.0 # bantu bangun path file db

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/logo.png
